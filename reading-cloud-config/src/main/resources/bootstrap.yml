server:
  port: 8888

spring:
  application:
    name: light-reading-cloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Zealon159/config-repo
          search-paths: dev,qa